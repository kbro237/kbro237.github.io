name: Cast
baseurl: /solar-theme-jekyll
description: A stylish blog using the Solarized color palette

permalink: /:year/:month/:day/:title/
paginate: 10
highlighter: rouge

exclude: ['README.md', 'Gemfile.lock', 'Gemfile', 'Rakefile']
plugins: [jekyll-paginate]
